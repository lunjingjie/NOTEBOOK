import { useState } from 'react';
import { ThemeMode } from './../../core/interfaces/action';

export const useThemeMode = () => {
	const [useThemeMode, setThemeMode] = useState<ThemeMode>('light');
  const engine = 
};
